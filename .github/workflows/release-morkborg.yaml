name: Release Module

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write
    uses: sheetdelver/sheetdelver/.github/workflows/module-release.yml@manifest-module-distribution
    with:
      module_id: 'morkborg'
      core_ref: 'manifest-module-distribution'
